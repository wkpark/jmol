topicdir = "biophysics/"
NIOcontentScript=topicdir + "biophysics.spt"
cd @topicdir

function loadModel(jmolFile,more,banner) {
  if (!jmolFile) { return }
  zap
  var msg = "banner:" + banner
  message @msg
  load @jmolFile
  if (more) { script inline @more }
  delay 120
}